<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Test">
    <!--可以重用的SQL块，也可以被其他语句引用-->
    <sql id="columns">
        id,name
    </sql>

    <select id="getAllTest" resultType="com.dk.mybatis.dao.Test">
        select <include refid="columns"/> from t_Test
    </select>

    <update id="createTable">
        create table t_hi (id int auto_increment primary key ,
                           name varchar(20) not null )
    </update>
    <!--TABLE_SCHEMA='dbname' and-->
    <!--com.dk.mybatis.dao.Test-->
    <select id="getTable" parameterType="String" resultType="Integer">
        select count(*) from INFORMATION_SCHEMA.TABLES where TABLE_NAME='${tablename}';
    </select>
</mapper>
